TypeScript並非一個全新語言，而是建立在 JavaScript基礎上添加了新的功能和優點
TypeScript將編寫的程式碼，編輯成 JavaScript 

優點：
強行別，可以更好辨識程式碼的內容，在執行時得到未想到的錯誤
例如：方法傳入的參數希望是兩個數字，但在 JavaScript中無法設定傳入的類型
可能讓你得到不想要的結果

缺點：
TypeScript無法由 JavaScript環境(例如 瀏覽器)執行

特性：
獨有的接口和泛型，並不影響編譯為 JavaScript，因為它不必編譯，
僅在開發中，更清楚的看到錯誤，並幫助我們避免更多錯誤